{
  "name": "@nimishjha/nimbus",
  "version": "1.0.0",
  "dependencies": {},
  "license": "GNU",
  "private": false,
  "scripts": {
    "lintandbuild": "bun run lint && bun run build.js",
    "lab": "bun run lint && bun run build.js",
    "lint": "oxlint -f stylish src/**/*.js",
    "lintconcise": "oxlint -f unix src/**/*.js",
    "lintverbose": "oxlint -f default src/**/*.js",
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.0.11"
  }
}
